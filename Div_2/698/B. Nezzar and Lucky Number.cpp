#include<bits/stdc++.h>
using namespace std;
using pii = pair<int, int>;
const int N = 1e3 + 1;
int n;
vector<pii> rect[N];
 
 
int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
 
	cin >> n;
 	cout << '\n';
 	return 0;
}